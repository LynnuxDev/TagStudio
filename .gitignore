# Secrets / local env — never commit these
.env
.env.keys

# Dependencies
node_modules

# Build outputs
dist

# Database (created at runtime)
data
**/*.db
!demo/Documents/db-example.db

# Generated thumbnail / remux caches written next to your files
**/.ts/

# Nix dev shell (local only)
.direnv/
result

# Logs / OS noise
*.log
.DS_Store
